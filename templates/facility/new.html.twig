{% extends 'base.html.twig' %}

{% block title %}Nouvelle facilité{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">Créer une nouvelle facilité</div>

                    <div class="card-body">
                        {{ include('facility/_form.html.twig') }}
                    </div>
                </div>
                <a href="{{ path('app_facility_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}

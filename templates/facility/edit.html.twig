{% extends 'base.html.twig' %}

{% block title %}Modifier la facilité{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">Modifier la facilité</div>

                    <div class="card-body">
                        {{ include('facility/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                    </div>
                </div>

                <div class="mt-3">
                    <a href="{{ path('app_facility_index') }}" class="btn btn-secondary mr-2">Retour à la liste</a>
                    {{ include('facility/_delete_form.html.twig') }}
          

                </div>
            </div>
        </div>
    </div>
{% endblock %}

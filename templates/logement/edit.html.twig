{% extends 'base.html.twig' %}

{% block title %}Edit House{% endblock %}

{% block body %}
    <h1>Edit House</h1>
   <h1>Ajouter un élément proposé</h1>

    {{ form_start(form) }}
    {{ form_row(form.name) }}
   
   

    <button type="submit">Ajouter</button>
    {{ form_end(form) }}
    {{ include('logement/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_logement_index') }}">back to list</a>
     <a href="{{ path('app_facility_new', {'id': house.id}) }}">Ajouter des équipements</a>
    <a href="{{ path('app_proprio_index') }}">Lien vers la page propriétaire</a>

    {{ include('logement/_delete_form.html.twig') }}
{% endblock %}
